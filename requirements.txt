# Reproducible dependency lockfile (validated in `conda` env: galore)
# Installs CUDA 12.1 GPU wheels from official PyTorch index.
--extra-index-url https://download.pytorch.org/whl/cu121

# Deep Learning Framework (GPU)
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# Transformers & Optimization
transformers==5.0.0
accelerate==1.12.0
bitsandbytes==0.49.1
galore-torch==1.0

# Training utilities
datasets==4.5.0
peft==0.18.1

# Configuration & Others
pyyaml==6.0.3
tqdm==4.67.3
numpy==1.26.4
safetensors==0.7.0

# Model Download (Optional - for China users)
modelscope==1.34.0
