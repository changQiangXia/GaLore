# Core dependencies for Qwen2-0.5B fine-tuning with GaLore on 4GB VRAM

# Deep Learning Framework
torch>=2.0.0
torchvision
torchaudio

# Transformers & Quantization
transformers>=4.40.0
accelerate>=0.27.0
bitsandbytes>=0.43.0

# Training utilities
datasets>=2.14.0
peft>=0.9.0

# Configuration & Others
pyyaml>=6.0.1
tqdm>=4.66.0
numpy<2.0.0
safetensors>=0.4.0

# Model Download (Optional - for China users)
modelscope>=1.15.0
